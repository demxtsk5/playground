$TTL    604800
; SOA record with MNAME and RNAME updated
@       IN      SOA     {{domain_name}}. root.{{domain_name}}. (
                              2         ; Serial Note: increment after each change
                         604800         ; Refresh
                          86400         ; Retry
                        2419200         ; Expire
                         604800 )       ; Negative Cache TTL
; Name server record 
@       IN      NS      k8s-infra.{{domain_name}}.
; A record for name server
k8s-infra      IN      A       192.168.56.101
; PTR record for name server
2   IN      PTR     bindserver.{{domain_name}}
; PTR record for clients
102   IN      PTR     k8s-m1.{{domain_name}}
103   IN      PTR     k8s-n1.{{domain_name}}
104   IN      PTR     k8s-n2.{{domain_name}}